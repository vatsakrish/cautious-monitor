# StoreMetricLogging service

Collects different metrics from other APIs, database tables, system details, and log files, and logs output to be captured by a Splunk forwarder	

## Build & Package

    mvn clean install